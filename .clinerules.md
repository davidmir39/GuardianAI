# Contexto del Proyecto
Proyecto: GuardianAI (Detección de Fraude Bancario).
Problema: Clasificación binaria con desbalanceo extremo (fraude < 1%).
Stack: Python, Pandas, Scikit-Learn, XGBoost, Imbalanced-learn (SMOTE), SHAP.

## REGLA DE ORO INQUEBRANTABLE
Antes de ejecutar cualquier comando, escribir código o redactar texto, **DEBES LEER el archivo `.professor_feedback.md`**. Todas tus acciones deben estar enfocadas en corregir los errores listados ahí.

# Reglas de Codificación y Estructura (Repositorio)
1. **Modularidad Estricta:** No escribas notebooks gigantes. Divide la lógica en módulos dentro de una carpeta `/src` (ej. `src/data_prep.py`, `src/models.py`, `src/evaluation.py`).
2. **Nombres Claros:** No uses nombres ambiguos como "hitos2_3". Usa nombres descriptivos.
3. **CERO EMOJIS:** Está estrictamente prohibido usar emojis en los mensajes de la terminal, `print()`, logs o comentarios. El tono debe ser 100% profesional y académico.

# Reglas Metodológicas de Machine Learning (CRÍTICO)
4. **Flujo de Preprocesamiento Correcto:** El preprocesamiento (escalado, limpieza, SMOTE) debe aplicarse PRIMERO. *Todos* los modelos deben recibir y evaluarse con los mismos datos preprocesados en igualdad de condiciones.
5. **Ajuste de Hiperparámetros Justo:** La optimización de hiperparámetros DEBE aplicarse a *todos* los modelos candidatos, no solo al mejor modelo base.
6. **Justificación de Decisiones:** - Siempre que se elija un modelo (ej. Random Forest o Redes Neuronales), documenta *exactamente* la arquitectura (tipo de red) y por qué se eligió.
   - Justifica el espacio de búsqueda de hiperparámetros (de dónde salen los rangos).
   - Explica *cómo* se calcula la importancia de las características (ej. Gini impurity, SHAP values).
7. **Terminología Precisa:** No inventes términos (como "ingeniería de grafos" si no se usan grafos).

# Reglas de Evaluación y Tono Académico
8. **Prudencia Absoluta:** No hagas afirmaciones grandilocuentes (ej. "superioridad estadística", "dominancia"). Sé conservador. Los resultados de detección de fraude (20-30% de precisión) son típicos por el desbalanceo, dilo claramente y no inventes que hay modelos del 90% de precisión técnica si no es cierto.
9. **Interpretación de Negocio:** Si un modelo tiene 20% de precisión (1 de cada 5 bloqueos es fraude real), evalúa si es apto para producción en base al *coste de negocio* (matriz de costes: coste de falso positivo vs fraude evitado).
10. **Visualización Clara:** - Incluye gráficas de convergencia / curvas de aprendizaje para todos los modelos.
    - Crea una tabla comparativa de Ventajas vs Debilidades de cada modelo.
    - Si muestras matrices de confusión normalizadas, explica detalladamente el porcentaje en el texto.
